# Kratom News Today

Daily briefing publication on the kratom industry. Built with Eleventy, deployed via GitHub Actions to Cloudways.

## Local development

```bash
npm install
npx @11ty/eleventy --serve
```

## Publishing workflow

See `agent-docs/publishing-workflow.md` for the automated publishing process.

## Architecture

See `agent-docs/build-process.md` for build and deploy details.
