/**
 * send-briefing-email.js — Phase 2 stub
 *
 * When implemented, this script will:
 * 1. Read the briefing markdown by slug
 * 2. Format it for email (HTML template)
 * 3. Call Klaviyo's API to trigger a campaign send
 *
 * Usage: node scripts/send-briefing-email.js --slug=<slug>
 */

console.log('Email send is not yet implemented. This is a phase 2 feature.');
console.log('See the pre-launch checklist for Klaviyo setup steps.');
process.exit(0);
